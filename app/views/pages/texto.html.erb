<header>
  <%= image_tag "logo.png", width: "60px" %>  
  <h1>MeuLivro.tk</h1>

  <div id="novo">
    <%= link_to "Inicio", root_path %>
  </div>
</header>

<%= form_for @text, url: texto_path do |t| %>
  <%= t.text_area :texto, :value => "#{@text.texto}" %><br>
  <%= t.submit "Salvar", id: "salvar" %>
<% end %>

